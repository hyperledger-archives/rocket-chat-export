---
# This is a sample configuration file for bts App which has 5 nodes.
network:
  # Network level configuration specifies the attributes required for each organization
  # to join an existing network.
  type: fabric
  version: 2.2.0                 # currently tested 1.4.0, 1.4.4 and 2.2.0

  frontend: disabled #Flag for frontend to enabled for nodes/peers

  #Environment section for Kubernetes setup
  env:
    type: "dev"              # tag for the environment. Important to run multiple flux on single cluster
    proxy: haproxy                  # values can be 'haproxy' or 'ambassador'
    ambassadorPorts: 15010,15020    # Any additional Ambassador ports can be given here, must be comma-separated without spaces, this is valid only if proxy='ambassador'
    loadBalancerSourceRanges: # (Optional) Default value is '0.0.0.0/0', this value can be changed to any other IP adres or list (comma-separated without spaces) of IP adresses, this is valid only if proxy='ambassador'
    retry_count: 20                 # Retry count for the checks
    external_dns: disabled           # Should be enabled if using external-dns for automatic route configuration

  # Docker registry details where images are stored. This will be used to create k8s secrets
  # Please ensure all required images are built and stored in this registry.
  # Do not check-in docker_password.
  docker:
    url: "index.docker.io/username"
    username: "username"
    password: "password"
  
  # Remote connection information for orderer (will be blank or removed for orderer hosting organization)
  # For RAFT consensus, have odd number (2n+1) of orderers for consensus agreement to have a majority.
  orderers:
    - orderer:
      type: orderer
      name: orderer1
      org_name: bts               #org_name should match one organization definition below in organizations: key            
      uri: orderer1.bts-net:7050   # Can be external or internal URI for orderer which should be reachable by all peers
      certificate: /home/blockchain-automation-framework/build/orderer1.crt           # Ensure that the directory exists
    - orderer:
      type: orderer
      name: orderer2
      org_name: bts               #org_name should match one organization definition below in organizations: key            
      uri: orderer2.bts-net:7050   # Can be external or internal URI for orderer which should be reachable by all peers
      certificate: /home/blockchain-automation-framework/build/orderer2.crt           # Ensure that the directory exists
    - orderer:
      type: orderer
      name: orderer3
      org_name: bts               #org_name should match one organization definition below in organizations: key            
      uri: orderer3.bts-net:7050   # Can be external or internal URI for orderer which should be reachable by all peers
      certificate: /home/blockchain-automation-framework/build/orderer3.crt           # Ensure that the directory exists\\

  # The channels defined for a network with participating peers in each channel
  channels:
  - channel:
    consortium: btsConsortium
    channel_name: AllChannel
    orderer: 
      name: bts
    participants:
    - organization:
      name: hospital
      type: creator       # creator organization will create the channel and instantiate chaincode, in addition to joining the channel and install chaincode
      org_status: new
      peers:
      - peer:
        name: peer0
        gossipAddress: peer0.hospital-net:7051  # External or internal URI of the gossip peer
        peerAddress: peer0.hospital-net.ambassador2.example.com:8443 # External URI of the peer
      ordererAddress: orderer1.bts-net:7050             # External or internal URI of the orderer
    - organization:      
      name: insurance
      type: joiner        # joiner organization will only join the channel and install chaincode
      org_status: new
      peers:
      - peer:
        name: peer0
        gossipAddress: peer0.insurance-net:7051
        peerAddress: peer0.insurance-net.ambassador1.example.com:8443 # External URI of the peer
      ordererAddress: orderer1.bts-net:7050
    endorsers:
      name:
      - hospital
      - insurance
      corepeerAddress:
      - peer0.hospital-net:7051
      - peer0.insurance-net:7051
    genesis:
      name: OrdererGenesis  

  # Allows specification of one or many organizations that will be connecting to a network.
  # If an organization is also hosting the root of the network (e.g. doorman, membership service, etc),
  # then these services should be listed in this section as well.
  organizations:

    # Specification for the 1st organization. Each organization maps to a VPC and a separate k8s cluster
    - organization:
      name: bts
      country: SA
      state: Makkah
      location: Jeddah
      subject: "O=Orderer,L=21.54/39.19/Jeddah,C=SA"
      type: orderer
      external_url_suffix: ambassador.example.com
      org_status: new
      cli: enabled
      ca_data:
        url: ca.bts-net:7054
        certificate: file/server.crt        # This has not been implemented in 0.2.0.0
  
      cloud_provider: aws   # Options: aws, azure, gcp, minikube
      aws:
        access_key: "access_key"        # AWS Access key, only used when cloud_provider=aws
        secret_key: "secret_key"        # AWS Secret key, only used when cloud_provider=aws
  
      # Kubernetes cluster deployment variables. The config file path and name has to be provided in case
      # the cluster has already been created.
      k8s:
        region: "region"
        context: "context"
        config_file: "/home/blockchain-automation-framework/build/config"

      # Hashicorp Vault server address and root-token. Vault should be unsealed.
      # Do not check-in root_token
      vault:
        url: "https://vault.example.com:8200"
        root_token: "token"
        secret_path: "secret"
      # Git Repo details which will be used by GitOps/Flux.
      # Do not check-in git_access_token
      gitops:
        git_ssh: "ssh://git@github.com/username/blockchain-automation-framework.git"         # Gitops ssh url for flux value files 
        branch: "master"           # Git branch where release is being made
        release_dir: "platforms/hyperledger-fabric/releases/dev" # Relative Path in the Git repo for flux sync per environment. 
        chart_source: "platforms/hyperledger-fabric/charts"     # Relative Path where the Helm charts are stored in Git repo
        git_push_url: "github.com/username/blockchain-automation-framework.git"   # Gitops https URL for git push  (without https://)
        username: "username"          # Git Service user who has rights to check-in in all branches
        password: "password"          # Git Server user password
        email: "email"                # Email to use in git config
        private_key: "/home/blockchain-automation-framework/build/bts_baf_git_v1"          # Path to private key file which has write-access to the git repo
     
      # Services maps to the pods that will be deployed on the k8s cluster
      # This sample is an orderer service and includes a zk-kafka consensus
      services:
        ca:
          name: ca
          subject: "/C=GB/ST=London/L=London/O=Orderer/CN=ca.bts-net"
          type: ca
          grpc:
            port: 7054
        
        consensus:
          name: raft
          type: broker        #This field is not consumed for raft consensus
          replicas: 4         #This field is not consumed for raft consensus
          grpc:
            port: 9092        #This field is not consumed for raft consensus
                
        orderers:
        # This sample has multiple orderers as an example.
        # You can use a single orderer for most production implementations.
        # For RAFT consensus, have odd number (2n+1) of orderers for consensus agreement to have a majority.
        - orderer:
          name: orderer1
          type: orderer
          consensus: raft
          grpc:
            port: 7050
        - orderer:
          name: orderer2
          type: orderer
          consensus: raft
          grpc:
            port: 7050
        - orderer:
          name: orderer3
          type: orderer
          consensus: raft
          grpc:
            port: 7050

    - organization:
      name: hospital
      country: SA
      state: Makkah
      location: Jeddah
      subject: "O=Hospital,OU=Hospital,L=21.54/39.19/Jeddah,C=SA"
      type: peer
      external_url_suffix: ambassador2.example.com
      org_status: new
      ca_data:
        url: ca.hospital-net:7054
        certificate: file/server.crt
      
      cloud_provider: aws   # Options: aws, azure, gcp, minikube
      aws:
        access_key: "access_key"        # AWS Access key, only used when cloud_provider=aws
        secret_key: "secret_key"        # AWS Secret key, only used when cloud_provider=aws
  
      # Kubernetes cluster deployment variables. The config file path and name has to be provided in case
      # the cluster has already been created.
      k8s:
        region: "region"
        context: "context"
        config_file: "/home/blockchain-automation-framework/build/config"

      # Hashicorp Vault server address and root-token. Vault should be unsealed.
      # Do not check-in root_token
      vault:
        url: "https://vault.example.com:8200"
        root_token: "token"
        secret_path: "secret"
      # Git Repo details which will be used by GitOps/Flux.
      # Do not check-in git_access_token
      gitops:
        git_ssh: "ssh://git@github.com/username/blockchain-automation-framework.git"         # Gitops ssh url for flux value files 
        branch: "master"           # Git branch where release is being made
        release_dir: "platforms/hyperledger-fabric/releases/dev" # Relative Path in the Git repo for flux sync per environment. 
        chart_source: "platforms/hyperledger-fabric/charts"     # Relative Path where the Helm charts are stored in Git repo
        git_push_url: "github.com/username/blockchain-automation-framework.git"   # Gitops https URL for git push  (without https://)
        username: "username"          # Git Service user who has rights to check-in in all branches
        password: "password"          # Git Server user password
        email: "email"                # Email to use in git config
        private_key: "/home/blockchain-automation-framework/build/bts_baf_git_v1"          # Path to private key file which has write-access to the git repo

      services:
        ca:
          name: ca
          subject: "/C=SA/ST=Makkah/L=Jeddah/O=Hospital/CN=ca.hospital-net"
          type: ca
          grpc:
            port: 7054
        peers:
        - peer:
          name: peer0          
          type: anchor    # This can be anchor/nonanchor. Atleast one peer should be anchor peer.    
          gossippeeraddress: peer0.hospital-net:7051 # Internal Address of the other peer in same Org for gossip, same peer if there is only one peer
          peerAddress: peer0.hospital-net.ambassador2.example.com:8443 # External URI of the peer
          certificate: "/home/blockchain-automation-framework/build/org2/ca.crt" # certificate path for peer
          cli: enabled      # Creates a peer cli pod depending upon the (enabled/disabled) tag.        
          grpc:
            port: 7051         
          events:
            port: 7053
          couchdb:
            port: 5984
          restserver:
            targetPort: 20001
            port: 20001 
          expressapi:
            targetPort: 3000
            port: 3000

    - organization:
      name: insurance
      country: SA
      state: Makkah
      location: Jeddah
      subject: "O=Insurance,OU=Insurance,L=21.54/39.17/Jeddah,C=SA"
      type: peer
      external_url_suffix: ambassador1.example.com
      org_status: new
      ca_data:
        url: ca.insurance-net:7054
        certificate: file/server.crt
      
      cloud_provider: aws   # Options: aws, azure, gcp, minikube
      aws:
        access_key: "access_key"        # AWS Access key, only used when cloud_provider=aws
        secret_key: "secret_key"        # AWS Secret key, only used when cloud_provider=aws
  
      # Kubernetes cluster deployment variables. The config file path and name has to be provided in case
      # the cluster has already been created.
      k8s:
        region: "region"
        context: "context"
        config_file: "/home/blockchain-automation-framework/build/config"

      # Hashicorp Vault server address and root-token. Vault should be unsealed.
      # Do not check-in root_token
      vault:
        url: "https://vault.example.com:8200"
        root_token: "token"
        secret_path: "secret"
      # Git Repo details which will be used by GitOps/Flux.
      # Do not check-in git_access_token
      gitops:
        git_ssh: "ssh://git@github.com/username/blockchain-automation-framework.git"         # Gitops ssh url for flux value files 
        branch: "master"           # Git branch where release is being made
        release_dir: "platforms/hyperledger-fabric/releases/dev" # Relative Path in the Git repo for flux sync per environment. 
        chart_source: "platforms/hyperledger-fabric/charts"     # Relative Path where the Helm charts are stored in Git repo
        git_push_url: "github.com/username/blockchain-automation-framework.git"   # Gitops https URL for git push  (without https://)
        username: "username"          # Git Service user who has rights to check-in in all branches
        password: "password"          # Git Server user password
        email: "email"                # Email to use in git config
        private_key: "/home/blockchain-automation-framework/build/bts_baf_git_v1"          # Path to private key file which has write-access to the git repo

      services:
        ca:
          name: ca
          subject: "/C=SA/ST=Makkah/L=Jeddah/O=Insurance/CN=ca.insurance-net"
          type: ca
          grpc:
            port: 7054
        peers:
        - peer:
          name: peer0          
          type: anchor    # This can be anchor/nonanchor. Atleast one peer should be anchor peer.    
          gossippeeraddress: peer0.insurance-net:7051 # Internal Address of the other peer in same Org for gossip, same peer if there is only one peer
          peerAddress: peer0.insurance-net.ambassador1.example.com:8443 # External URI of the peer
          certificate: "/home/blockchain-automation-framework/build/org1/ca.crt" # certificate path for peer
          cli: enabled      # Creates a peer cli pod depending upon the (enabled/disabled) tag.        
          grpc:
            port: 7051         
          events:
            port: 7053
          couchdb:
            port: 5984
          restserver:
            targetPort: 20001
            port: 20001 
          expressapi:
            targetPort: 3000
            port: 3000